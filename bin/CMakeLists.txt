set(HEADER_DIR "../include")
cmake_minimum_required(VERSION 2.8)
project( DisplayImage)
find_package( opencv REQUIRED )
include_directories(${OpenCV_INCLUDE_DIRS})
add_executable( DisplayImage ../src/DisplayImage.cpp )
target_link_libraries( DisplayImage ${OpenCV_LIBS} )

project( test)
include_directories(${HEADER_DIR})
include_directories(${OpenCV_INCLUDE_DIRS})
set(SOURCES ../src/test.cpp ${HEADER_DIR}/camera.hpp)
set(TARGET_H ${HEADER_DIR}/camera.hpp)
find_package( opencv REQUIRED )
add_executable( Harris ../src/test.cpp ../src/camera.cpp)
target_link_libraries( Harris ${OpenCV_LIBS} )

project( stitch_test)
include_directories(../include)
include_directories(${OpenCV_INCLUDE_DIRS})
set(SOURCES ../src/stich_test.cpp ${HEADER_DIR}/camera.hpp)
set(TARGET_H ${HEADER_DIR}/camera.hpp)
find_package( opencv REQUIRED )
add_executable( Stitch_test ../src/stitch_test.cpp ../src/camera.cpp)
target_link_libraries( Stitch_test ${OpenCV_LIBS})

project( ptz_test)
include_directories(../include)
include_directories(${OpenCV_INCLUDE_DIRS})
set(SOURCES ../src/ptz.cpp ${HEADER_DIR}/camera.hpp)
set(TARGET_H ${HEADER_DIR}/camera.hpp)
find_package( opencv REQUIRED )
add_executable( ptz_test ../src/ptz.cpp ../src/camera.cpp)
target_link_libraries( ptz_test ${OpenCV_LIBS})

project( surf_test)
include_directories(../include)
include_directories(${OpenCV_INCLUDE_DIRS})
set(SOURCES ../src/surf.cpp ${HEADER_DIR}/camera.hpp)
set(TARGET_H ${HEADER_DIR}/camera.hpp)
find_package( opencv REQUIRED )
add_executable( Surf_test ../src/Surf.cpp ../src/camera.cpp)
target_link_libraries( Stitch_test ${OpenCV_LIBS})
